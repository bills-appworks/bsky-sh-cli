# SC1091 disable for dynamical(variable) path source(.) using
external-sources=true

# recommendation expr -> $((...)) disable for compatible with Solaris sh
disable=SC2003

# recommendation `...` -> $(...) disable for compatible with Solaris sh
disable=SC2006

# recommendation var=$@ -> var=("$@") disable for shells that do not support arrays
disable=SC2124
